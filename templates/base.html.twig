<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, user-scalable=no, minimum-scale=1.0, maximum-scale=1.0">
        <title>NGS{% block title %}{% endblock %}</title>
        <script src="https://kit.fontawesome.com/12c728ad22.js" crossorigin="anonymous"></script>
        <link href="https://fonts.googleapis.com/css2?family=Noto+Serif:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700;800&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
        <link rel="stylesheet" href="{{asset('assets/css/style.css')}}">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light bg-white nav-fix">
            <a class="navbar-brand logo" href="{{ path("home_index") }}">NGS</a>
            <div class="d-flex">
                <div class="navbar-nav mx-4 nav-user d-flex justify-content-center">
                    <a class="nav-link mr-4 text-dark d-lg-none mr-lg-1" href="{{ path('product_search') }}"><i class="fas fa-search"></i></a>
                    {% if is_granted('ROLE_SUBSCRIBER') %}
                    <a class="nav-link mr-2 text-dark d-lg-none mr-lg-1" href="{{ path('profile') }}"><i class="fas fa-user"></i></a>
                    {% else %}
                    <a class="nav-link mr-3 text-dark d-lg-none mr-lg-1" href="{{ path('app_login') }}"><i class="fas fa-user"></i></a>
                    {% endif %}
                    <a class="nav-link ml-3 text-dark d-lg-none ml-lg-1" href="{{ path('cart_index') }}"><i class="fas fa-shopping-cart"></i></a>
                </div>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>
            <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
                <ul class="navbar-nav">
                    {% for category in categories %}
                    <li class="nav-item dropdown">
                        <a class="nav-link text-dark dropbtn" href="{{ path('category_index', {'id': category.id}) }}">{{ category.name }}</a>
                        <ul class="navbar-nav dropdown-content">
                            {% for subCateg in category.subCategories %}
                            <li class="nav-item"><a href="{{ path('subCategory_index', {'id': subCateg.id}) }}">{{ subCateg.name }}</a></li>
                            {% endfor %}
                        </ul>
                    </li>
                    {% endfor %}
                    <li class="nav-item">
                        <a class="nav-link text-dark dropbtn" href="#">Team</a>
                    </li>
                </ul>
                <div class="d-md-flex">
                    <div class="navbar-nav mx-4 nav-user d-flex justify-content-center">
                        <a class="nav-link d-none text-dark d-lg-block mr-md-3" href="{{ path('product_search') }}"><i class="fas fa-search"></i></a>
                        {% if is_granted('ROLE_SUBSCRIBER') %}
                        <a class="nav-link mr-3 d-none text-dark d-lg-block mr-md-1" href="{{ path('profile') }}"><i class="fas fa-user"></i></a>
                        {% else %}
                        <a class="nav-link mr-3 d-none text-dark d-lg-block mr-md-1" href="{{ path('app_login') }}"><i class="fas fa-user"></i></a>
                        {% endif %}
                        <a class="nav-link ml-3 d-none text-dark d-lg-block ml-md-1" href="{{ path('cart_index') }}"><i class="fas fa-shopping-cart"></i></a>
                    </div>
                </div>
            </div>
        </nav>
        <div class="main">
        {% block body %}
        {% endblock %}
        </div>
        <nav class="navbar navbar-dark mt-5 bg-dark footer justify-content-center">
            <a class="navbar-brand logo" href="#">NGS</a>
            <div class="d-flex">
                <a class="btn nav-item nav-link text-white" href="#">Contact</a>
                <a class="btn nav-item nav-link text-white" href="#">About Us</a>
            </div>
            <div class="d-flex network">
                <a href="https://www.youtube.com" target="_blank" class="btn nav-item nav-link"><i class="fa fa-youtube-square fa-2x" aria-hidden="true"></i></a>
                <a href="https://twitter.com" target="_blank" class="btn nav-item nav-link ml-3"><i class="fa fa-twitter-square fa-2x" aria-hidden="true"></i></a>
                <a href="https://www.facebook.com" target="_blank" class="btn nav-item nav-link ml-3"><i class="fa fa-facebook-square fa-2x" aria-hidden="true"></i></a>
            </div>
            <span class="navbar-text copyright">NewGenerationSupreme, Copyright © 2020</span>
        </nav>
        {% block javascripts %}{% endblock %}
        <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
    </body>
</html>
