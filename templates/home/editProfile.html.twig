{% extends 'base.html.twig' %}

{% block title %}-Profile{% endblock %}

{% block body %}
    {% include 'home/news.html.twig' %}
    <div class="container col-md-6">
        <h1 class="text-center my-5">Modifier ton Profile</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_rest(form) }}
        <button class="btn btn-success">{{ button_label|default('Modifier') }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
